import { createStyles, makeStyles } from '@material-ui/core'

export const useStyles = makeStyles(() =>
  createStyles({
    loadingContainer: {
      marginTop: 32,
    },
    container: {
      marginTop: 16,
    },
  }),
)
